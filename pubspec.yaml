name: tipzty_flutter_ui
description: Tipzty Flutter UI

publish_to: "none"

version: 1.0.0

environment:
  sdk: ">=2.8.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations: # Add this line
    sdk: flutter # Add this line

  cupertino_icons: ^1.0.1
  websafe_svg: ^1.1.4
  google_fonts: ^1.1.1
  cached_network_image: ^2.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true